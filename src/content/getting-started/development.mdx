---
title: 'Development'
date: '2024-06-28'
description: 'Learn how to quickly get started with this documentation template.'
draft: false
type: getting-started
---

import ExternalLink from '../../components/docs/ExternalLink.astro'
import { Code } from 'astro:components'
import Card from '../../components/Card.astro'
import CardGrid from '../../components/docs/CardGrid.astro'
import GroupName from '../../components/docs/GroupName.astro'
import Callout from '../../components/docs/Callout'
import DocsAccordion from '../../components/docs/Accordion'
import CodeBlock from '../../components/docs/CodeBlock.astro'

<GroupName>Getting Started</GroupName>

## Development

Learn how to preview changes locally

You can combine these. For example, write `**_bold and italic_**` to get bold and italic text. You can combine these. For example, write. You can combine these. For example, write. You can combine these. For example, write

<Callout
  alertTitle='Heads up!'
  alertDescription='You can add components to your app using the cli.'
  variant='callout'
/>

<DocsAccordion client:load />

This is some dummy text

> You can combine these. For example, write `**_bold and italic_**` to get bold and italic text. You can combine <ExternalLink url='https://google.com'>Google</ExternalLink>. For example, write. You can combine these. For example, write. You can combine these. For example, write

<CodeBlock
  tabs={[
    { label: 'npm', code: 'npm install @astrojs/react', lang: 'bash' },
    { label: 'yarn', code: 'yarn add @astrojs/react', lang: 'bash' },
    { label: 'pnpm', code: 'pnpm add @astrojs/react', lang: 'bash' },
  ]}
/>

<CodeBlock
  tabs={[
    { label: 'JavaScript', code: `function greet(name) {
  console.log(\`Hello, \${name}!\`);
}

greet('Astro');
`, lang: 'javascript' },
    { label: 'Python', code: `def greet(name):
print(f"Hello, {name}!")

greet("Astro")
`, lang: 'python' },
]}
/>
